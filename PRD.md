# LexiAnchor 产品需求文档（PRD）

> 版本：v1.0
>
> 状态：需求基线
>
> 日期：2026-07-24
>
> 产品类型：本地优先、跨平台的 EPUB/PDF 英语学习阅读器
>
> 目标平台：Windows、macOS、Web/PWA

## 1. 文档目的

本文档定义 LexiAnchor 首个可用版本（MVP）的产品目标、范围、交互、数据、非功能要求、验收标准和后续演进方向，作为产品设计、技术方案、开发拆分和测试验收的共同基线。

## 2. 产品概述

LexiAnchor 是一款面向英语学习者、尤其是存在轻度阅读障碍或注意力保持困难用户的现代电子书阅读器。

产品通过可调节的“焦点加粗阅读”、本地词典、离线优先翻译、词卡收藏和低干扰阅读界面，帮助用户：

- 更稳定地跟随英文文本；
- 减少阅读过程中的注意力丢失；
- 在不中断阅读的情况下查词和翻译；
- 将阅读中遇到的词汇沉淀为可搜索的个人词卡；
- 在 Windows、macOS 和 Web 上获得一致的核心体验。

LexiAnchor 采用本地优先策略。书籍、阅读进度、设置、词典和词卡默认保存在用户设备上，无需注册账号即可使用。数据模型和业务层需要预留跨设备同步接口，但 MVP 不提供云账号和跨设备同步服务。

## 3. 背景与问题

英语学习者在阅读 EPUB 或 PDF 时通常需要在阅读器、词典、翻译网站和单词本之间频繁切换。对于存在轻度阅读障碍或注意力困难的用户，这些上下文切换会进一步破坏阅读节奏。

现有产品常见的问题包括：

- 文字显示缺少针对注意力和阅读障碍的辅助能力；
- 查词、翻译和收藏分散在多个工具中；
- 依赖联网服务，离线时核心学习流程不可用；
- 桌面端和 Web 端体验割裂；
- 阅读设置不够细致，难以找到适合个人的排版；
- 用户无法控制书籍、词卡和阅读记录的存储位置与隐私。

## 4. 目标用户

### 4.1 核心用户

首要用户是产品创建者本人：

- 正在通过英文原版书学习英语；
- 存在轻度阅读障碍；
- 希望使用视觉锚点保持阅读注意力；
- 经常需要英英、英法和英汉词典；
- 希望书籍、词典和词卡尽量保存在本地；
- 需要在 macOS、Windows 和 Web 环境中使用。

### 4.2 扩展用户

- 希望通过英文原版书扩大词汇量的中高级英语学习者；
- 阅读长篇英文文本时容易丢行或注意力中断的用户；
- 重视离线使用和数据隐私的电子书用户；
- 同时使用英语、法语和中文辅助学习的用户。

## 5. 产品原则

1. **阅读优先**：任何学习工具都不应让用户失去当前阅读位置。
2. **本地优先**：无账号也能完成导入、阅读、查词、翻译和词卡收藏。
3. **好默认值优先**：开箱即用，不要求用户先理解大量排版参数。
4. **渐进式设置**：常用选项直接可见，高级选项收纳在二级界面。
5. **低干扰反馈**：高频操作即时完成，避免冗长或装饰性动画。
6. **无障碍不是附加项**：键盘操作、清晰焦点、对比度和减少动态效果属于基础要求。
7. **提供者可替换**：词典、翻译、搜索和未来同步均通过统一接口接入。
8. **不夸大效果**：不宣称能够治疗阅读障碍，也不保证提高阅读速度或理解力。

## 6. 产品目标与非目标

### 6.1 MVP 目标

- 支持无 DRM 的 EPUB 2/3 和带文本层的 PDF。
- 提供可开关、可调强度的焦点加粗阅读。
- 内置可离线使用的英英、英法、英汉词典。
- 允许用户安装自己的离线词典。
- 支持单词、短语和句子的本地优先翻译。
- 始终提供在线翻译和 Search on Web 入口。
- 支持将查词结果添加为词卡。
- 提供首页、书库、词卡和阅读器完整闭环。
- 支持浅色、深色、护眼主题和现代阅读排版设置。
- 支持窗口阅读与全屏阅读。
- 支持简体中文、英文和法文 UI，默认跟随系统，不匹配时使用英文。
- 通过共享核心代码支持 Windows、macOS 和 Web/PWA。

### 6.2 MVP 非目标

- 不破解或绕过 DRM。
- 不支持扫描版 PDF 的 OCR。
- 不对 PDF 执行 EPUB 式全文重排。
- 不支持 Kindle 专有格式、漫画格式或音频书。
- 不提供账号、云端书库或正式跨设备同步。
- 不提供社交、排行榜或公开词卡社区。
- 不提供完整的间隔重复复习系统。
- 不提供电子书购买或版权内容分发。
- 不默认上传书籍全文到任何服务器。

## 7. 名词定义

### 7.1 无 DRM 文件

无 DRM 文件是未通过账号、设备或专用阅读软件进行加密绑定，并可由标准解析器读取的 EPUB/PDF 文件。

LexiAnchor 只读取用户合法取得且未加密的文件。应用不得提供 DRM 检测绕过、密钥提取或解密功能。

### 7.2 焦点加粗阅读

一种独立实现的辅助排版能力：按照可配置规则加粗单词的一部分，为视线提供视觉锚点。

在完成知识产权审查或取得书面授权前：

- 产品 UI、代码、仓库名和宣传文案统一使用“焦点加粗阅读”或 “Focus Highlighting”；
- 不将 “Bionic Reading®” 作为功能名、产品卖点或兼容性声明；
- 不使用其字体、图标、代码、品牌资产、API 或反向工程结果。

### 7.3 本地优先

核心功能和主要数据默认在设备本地运行和保存。只有用户主动选择在线翻译、网页搜索或未来同步时，相关最小必要内容才可离开设备。

## 8. 信息架构

主导航位于可折叠侧边栏，至少包含：

1. **Home**
2. **书库**
3. **词卡**
4. **设置**

阅读器为独立工作区。打开书籍后，应尽量扩大正文面积；侧边栏和工具栏均可折叠。

## 9. 核心用户流程

### 9.1 首次使用

1. 应用读取系统语言。
2. 若系统语言为简体中文、英文或法文，则使用对应 UI；否则使用英文。
3. 用户可跳过账号步骤，因为 MVP 不要求账号。
4. 用户导入 EPUB/PDF，或先进入示例阅读页面体验排版设置。
5. 应用提示本地词典状态，并允许下载或启用预装词典包。
6. 应用进入 Home，展示最近导入或最近阅读的书。

### 9.2 继续阅读

1. 用户在 Home 点击最近阅读的书。
2. 阅读器恢复上次位置、主题和该书的排版设置。
3. 书籍卡片和阅读器中均展示百分比进度。
4. 进度在翻页、滚动停止或窗口失焦时自动保存。

### 9.3 查词与翻译

1. 用户选择单词、短语或句子。
2. 桌面端支持右键菜单；Web 和触摸环境同时支持浮动操作栏。
3. 用户选择“查词/翻译”。
4. 系统优先返回本地词典或本地翻译结果。
5. 用户可切换词典结果、在线翻译或 Search on Web。
6. 翻译面板关闭后，正文选择和阅读位置不应异常跳动。

### 9.4 添加词卡

1. 用户在翻译/词典面板点击“添加到词卡”。
2. 系统预填词语、词性、英语释义、词根、来源书籍、原句和创建时间。
3. 用户可在保存前编辑字段。
4. 保存成功后给予轻量反馈，不强制离开阅读器。
5. 用户可在侧边栏“词卡”中搜索、查看和删除该记录。

## 10. 功能需求

### 10.1 Home

#### FR-HOME-01 最近在读

- 展示最近阅读的书籍。
- 默认按最后阅读时间倒序排列。
- 首屏建议展示 4–8 本，具体数量随窗口宽度响应式调整。
- 每本书至少展示封面、书名、作者、格式和阅读进度。
- 点击书籍后恢复到上次阅读位置。

#### FR-HOME-02 阅读进度

- 使用百分比展示，范围为 `0%–100%`。
- 未开始阅读显示 `0%`。
- EPUB 按当前位置相对全书可定位内容计算。
- PDF 按当前页和页内位置计算。
- 进度不得因字体或窗口尺寸变化而明显倒退。

#### FR-HOME-03 空状态

- 无书籍时解释如何导入 EPUB/PDF。
- 提供明确的“导入书籍”主按钮。
- 可提供不涉及版权的内置示例文本。

### 10.2 书库

#### FR-LIB-01 导入

- 桌面端支持文件选择器和拖放导入。
- Web 端支持浏览器文件选择器；如浏览器支持，可使用 File System Access API，但必须提供标准文件选择回退。
- 支持批量导入。
- 导入时验证格式、文件完整性和 DRM/加密状态。
- 无法读取时给出可理解的错误原因，不删除原文件。

#### FR-LIB-02 支持格式

- EPUB 2、EPUB 3，无 DRM。
- 带文本层的 PDF，无密码或已由用户合法解锁。
- 扫描版或图片型 PDF 可打开浏览，但 MVP 不承诺查词、全文搜索或焦点加粗。

#### FR-LIB-03 书籍元数据

- 自动读取书名、作者、封面、语言和出版信息。
- 元数据缺失时使用文件名和生成的占位封面。
- 用户可编辑书名、作者和封面。

#### FR-LIB-04 管理能力

- 支持按书名或作者搜索。
- 支持按最近阅读、导入时间、书名和进度排序。
- 支持网格和列表视图。
- 删除书籍前说明将删除哪些本地数据。
- 删除操作需要确认；阅读记录和词卡的处理方式必须可选择。

### 10.3 EPUB 阅读器

#### FR-EPUB-01 内容渲染

- 正确显示章节、段落、图片、链接、脚注和目录。
- 保留语义结构，同时允许用户样式覆盖正文排版。
- 阻止 EPUB 内不可信脚本访问宿主能力。

#### FR-EPUB-02 导航

- 支持目录跳转、上一章、下一章、翻页或连续滚动。
- 支持键盘方向键、Page Up/Page Down 和可配置快捷键。
- 章节切换后更新并保存阅读位置。

#### FR-EPUB-03 位置恢复

- 位置标识不依赖单纯像素偏移。
- 字体大小、列宽或窗口大小改变后，尽量恢复到同一语义位置。

### 10.4 PDF 阅读器

#### FR-PDF-01 显示方式

- 保持 PDF 原始版式。
- 支持单页、连续滚动、适合页宽和适合页面。
- 支持缩放、旋转和页码跳转。
- MVP 不进行全文重排。

#### FR-PDF-02 文本能力

- 带文本层的 PDF 支持选择、复制、查词、翻译和网页搜索。
- 焦点加粗优先通过文本层覆盖实现，不永久修改原文件。
- 对文本层错位或缺失的页面显示能力受限提示。

#### FR-PDF-03 扫描文件

- 图片型 PDF 可以作为页面查看。
- 清楚提示“未检测到可选择文本”。
- OCR 作为后续版本能力，不在 MVP 中承诺。

### 10.5 焦点加粗阅读

#### FR-FOCUS-01 开关

- 阅读工具栏提供快速开关。
- 设置按书籍保存，同时有全局默认值。
- 开关切换不应改变阅读位置。

#### FR-FOCUS-02 强度

- MVP 提供“弱、中、强”三级。
- 默认使用“中”。
- 强度可映射到加粗字符比例、字重差异和短词处理规则。
- 高级算法参数不直接暴露在主工具栏。

#### FR-FOCUS-03 文本保护

- 不处理代码、URL、页码、目录编号或用户明确排除的内容。
- 已有粗体、斜体、链接和脚注的语义必须保留。
- 复制文本时得到原始文字，不包含为渲染添加的标记。
- 搜索、选择、屏幕阅读器和词典识别使用原始文本。

#### FR-FOCUS-04 可访问性

- 焦点加粗不是强制模式，用户可随时关闭。
- 不使用闪烁、自动滚动或持续运动。
- 不将该功能描述为医疗治疗。

### 10.6 阅读显示设置

#### FR-DISPLAY-01 主题

至少提供：

- 浅色主题；
- 深色主题；
- 护眼主题。

主题分别定义背景、正文、弱化文字、链接、选中态和面板颜色，保证足够对比度。

#### FR-DISPLAY-02 排版

EPUB 至少支持调节：

- 字体大小；
- 字体家族；
- 字重；
- 行距；
- 字间距；
- 字母间距；
- 段落间距；
- 页面边距；
- 正文最大宽度；
- 左对齐/两端对齐；
- 单栏/双栏（窗口足够宽时）。

PDF 至少支持：

- 缩放；
- 页间距；
- 适合页宽/页面；
- 页面背景周边颜色；
- 在能力允许时应用主题覆盖。

#### FR-DISPLAY-03 预设和重置

- 提供适合普通阅读的默认预设。
- 提供针对阅读困难的高可读性预设。
- 支持恢复默认值。
- 设置修改应实时预览，但不得造成明显卡顿。

### 10.7 阅读模式

#### FR-MODE-01 窗口模式

- 完整展示侧边栏、标题栏和阅读工具。
- 用户可折叠侧边栏。
- 窗口尺寸变化时正文自动重新布局。

#### FR-MODE-02 全屏模式

- 桌面端进入系统全屏或无边框沉浸模式。
- Web 端优先使用 Fullscreen API；不支持时使用页面内沉浸模式。
- 默认隐藏导航和非必要工具。
- 鼠标移动至边缘、点击或使用快捷键时显示工具。
- `Esc` 可退出全屏或关闭最上层面板。

#### FR-MODE-03 阅读连续性

- 窗口与全屏切换不得丢失阅读位置。
- 高频快捷键操作应即时响应，不增加装饰性过渡。

### 10.8 本地词典

#### FR-DICT-01 内置词典

至少提供三类离线词典：

- 英英；
- 英法；
- 英汉。

每个词典包必须记录：

- 名称；
- 语言方向；
- 版本；
- 来源；
- 许可证；
- 安装状态；
- 数据更新时间。

只有许可证允许应用分发或按需下载的数据才能作为官方词典包。

#### FR-DICT-02 用户词典

- 支持用户从本地安装词典包。
- MVP 至少支持一种公开、可文档化的离线词典格式。
- 优先调研 StarDict 作为首个兼容格式。
- MDX/MDD 兼容性列为 P1 技术调研，不应在未确认许可和解析质量前承诺。
- 导入失败不得破坏现有词典。
- 用户词典的内容和许可证由用户负责，应用应显示提示。

#### FR-DICT-03 查询顺序

- 设置中以拖放或键盘可操作方式调整词典顺序。
- 可单独启用或停用某个词典。
- 查询结果按设定顺序展示。
- 无结果时自动继续查询下一个已启用词典。

#### FR-DICT-04 词条内容

尽可能展示：

- 词形；
- 音标；
- 词性；
- 英语释义；
- 法语或中文释义；
- 词根/词源；
- 例句；
- 词典来源。

字段不存在时不得编造内容，应显示为空或“该词典未提供”。

### 10.9 翻译与网页搜索

#### FR-TRANS-01 选择范围

- 支持单词、短语和句子。
- 选择超长段落时提示截断或要求用户确认。
- 默认保留原句上下文，但只处理用户选中的内容。

#### FR-TRANS-02 本地优先路由

处理优先级：

1. 单词先查本地词典；
2. 短语和句子调用已安装的本地翻译模型；
3. 本地能力缺失时显示原因和在线翻译选项；
4. 不在用户未操作的情况下自动上传内容。

本地翻译模型通过可下载语言包提供，MVP 优先覆盖：

- 英语 → 简体中文；
- 英语 → 法语；
- 法语 → 英语；
- 简体中文 → 英语（可根据模型体积降为 P1）。

具体模型、体积、许可证和硬件性能需要在开发前完成技术验证。

#### FR-TRANS-03 在线翻译

- 所有支持的选择范围始终提供“在线翻译”入口。
- 在线翻译提供者可配置和替换。
- 首次使用某个在线服务时提示将发送的文本和目标服务。
- API 密钥不得硬编码进公开仓库。
- 用户应能关闭或移除在线翻译提供者。

#### FR-TRANS-04 Search on Web

- 翻译面板和选中文本菜单始终提供 `Search on Web`。
- 默认搜索提供者可配置。
- 搜索前对选中文本进行安全 URL 编码。
- Web 端在新标签页打开；桌面端使用系统默认浏览器。
- 不将整本书、文件路径或多余上下文加入搜索请求。

#### FR-TRANS-05 错误与降级

- 离线时清楚区分“没有网络”和“在线服务失败”。
- 本地模型未安装时提供下载入口。
- 翻译失败不影响原文阅读和词典查询。

### 10.10 词卡

#### FR-CARD-01 创建

词卡保存以下字段：

- 词语；
- 词性；
- 英语释义；
- 词根或词源；
- 来源书籍；
- 原句；
- 创建时间。

补充内部字段：

- 唯一 ID；
- 来源书籍 ID；
- 来源位置；
- 数据来源词典；
- 更新时间；
- 删除标记；
- 数据版本。

词根或词源在数据源未提供时允许为空，并允许用户手动填写。不得通过猜测自动生成。

#### FR-CARD-02 去重

- 同一词语重复收藏时给予提示。
- 用户可选择更新现有词卡、保留多条不同语境词卡或取消。
- 不同词性和不同原句可以保留为独立语境记录。

#### FR-CARD-03 管理

- 支持按词语、释义、词根、书名和原句搜索。
- 支持按创建时间、词语和来源书籍排序。
- 支持查看详情和编辑。
- 支持删除；删除需要可撤销提示或回收站机制。

#### FR-CARD-04 后续扩展

以下能力不属于 MVP，但数据模型不得阻碍后续实现：

- 标签；
- 发音；
- 自定义笔记；
- 间隔重复复习；
- 熟悉度和复习记录；
- CSV/Anki 导出；
- 跨设备同步。

### 10.11 设置

设置至少分为：

- 外观与排版；
- 阅读与焦点加粗；
- 词典；
- 翻译与网页搜索；
- 语言；
- 存储与隐私；
- 快捷键；
- 关于与许可证。

所有设置必须说明作用范围：

- 全局默认；
- 当前书籍覆盖；
- 当前设备；
- 未来可同步。

### 10.12 UI 本地化

#### FR-I18N-01 支持语言

MVP UI 支持：

- English；
- 简体中文；
- Français。

#### FR-I18N-02 默认逻辑

1. 首次启动读取系统或浏览器语言。
2. 完全匹配或语言族匹配到支持语言时，使用对应翻译。
3. 无匹配时使用英文。
4. 用户手动选择后，以用户选择为准，不再被系统变化覆盖。

#### FR-I18N-03 文案要求

- UI 语言与书籍语言、词典语言、翻译目标语言相互独立。
- 不允许关键按钮因翻译缺失而显示内部 key。
- 日期、数字和百分比使用当前 UI locale 格式化。

## 11. 交互与视觉要求

### 11.1 阅读界面

- 正文是视觉层级最高的区域。
- 控件使用克制的中性色，不与正文争夺注意力。
- 选词面板应靠近选择区域，但不能遮住整句或造成页面跳动。
- 工具栏在熟悉后可仅显示图标，但必须有可访问名称和工具提示。
- 破坏阅读状态的操作应有确认；普通设置切换不应弹出确认。

### 11.2 动效

- 键盘触发的高频操作不使用开关动画。
- 小型弹出层建议在 125–200ms 内完成。
- 抽屉和模态框建议在 200–300ms 内完成。
- 进入使用快速响应的 ease-out，位置移动使用 ease-in-out。
- 优先只动画 `transform` 和 `opacity`。
- 支持 `prefers-reduced-motion`，减少位移动效但保留必要的状态反馈。

### 11.3 响应式布局

- 宽屏：侧边栏 + 阅读正文 + 可选词典面板。
- 中等窗口：侧边栏可折叠，词典以覆盖面板显示。
- 窄屏/Web 移动端：底部或抽屉式导航，阅读正文单栏。
- MVP 主要保证桌面浏览器体验；移动 Web 可用但不作为独立原生移动应用交付。

## 12. 无障碍要求

- 所有核心流程均可用键盘完成。
- 焦点顺序与视觉顺序一致。
- 所有按钮、开关和滑块具有可访问名称和状态。
- 支持屏幕阅读器读取原始文本，不逐字读取渲染标记。
- 正文和 UI 颜色满足 WCAG 2.2 AA 对比度目标。
- 不仅依赖颜色表达状态。
- 主题、字体和间距调整不破坏缩放至 200% 的可用性。
- 提供清晰且始终可见的键盘焦点。
- 遵循系统减少动态效果设置。
- 不默认启用自动翻页、闪烁或持续动画。

## 13. 数据与存储

### 13.1 核心实体

#### Book

- `id`
- `title`
- `author`
- `format`
- `language`
- `coverRef`
- `contentRef`
- `contentHash`
- `importedAt`
- `lastOpenedAt`
- `metadata`

#### ReadingProgress

- `id`
- `bookId`
- `locator`
- `percentage`
- `updatedAt`
- `deviceId`
- `version`

#### ReaderPreferences

- `id`
- `scope`（global/book/device）
- `scopeId`
- `theme`
- `font`
- `fontSize`
- `lineHeight`
- `wordSpacing`
- `letterSpacing`
- `paragraphSpacing`
- `contentWidth`
- `focusHighlightingEnabled`
- `focusHighlightingLevel`
- `updatedAt`

#### DictionaryPackage

- `id`
- `name`
- `languages`
- `version`
- `source`
- `license`
- `format`
- `priority`
- `enabled`
- `installedAt`

#### WordCard

- `id`
- `term`
- `partOfSpeech`
- `englishDefinition`
- `rootOrEtymology`
- `sourceBookId`
- `sourceBookTitleSnapshot`
- `sourceSentence`
- `sourceLocator`
- `dictionarySource`
- `createdAt`
- `updatedAt`
- `deletedAt`
- `version`

### 13.2 本地存储策略

- 桌面端使用应用私有数据目录保存数据库、书籍副本或文件引用、词典和模型。
- Web 与桌面端的结构化数据使用相同 SQLite schema、migration 和 Repository API；SQLite WASM 在专用 Worker 中运行并持久化到 OPFS。
- Web 端的书籍、词典包和模型等大型内容通过 OPFS 保存，桌面端通过应用私有数据目录保存；两端由相同 `ContentStore` 接口封装。
- 浏览器清理站点数据可能导致本地数据丢失，Web 端必须提示并提供备份能力规划。
- 数据库变更必须使用版本化迁移。
- 书籍正文、词典包和翻译模型与结构化元数据分开管理。

### 13.3 同步预留

MVP 不实现跨设备同步，但核心业务不能直接依赖单一存储实现。至少预留：

- `SyncProvider` 接口；
- 稳定的全局唯一 ID；
- `createdAt`、`updatedAt`、`deletedAt`；
- 数据版本和迁移版本；
- 冲突解决所需的设备 ID；
- 软删除/tombstone；
- 可序列化的阅读位置；
- 可导入导出的用户数据包。

同步接口不得在 MVP 中暗中上传数据或创建账号。

## 14. 提供者接口

为降低后续替换成本，产品应抽象以下能力：

- `DictionaryProvider`
- `TranslationProvider`
- `SearchProvider`
- `LocalModelProvider`
- `StorageProvider`
- `SyncProvider`
- `BookParser`
- `FocusFormatter`

UI 不直接依赖具体词典文件、在线翻译厂商或云同步服务。

## 15. 隐私与安全

- 默认不收集遥测，不要求账号。
- 默认不上传书籍、阅读进度、词卡或查询历史。
- 在线翻译只发送用户明确选中的最小文本。
- 使用在线服务前显示提供者和隐私提示。
- API 密钥通过本地安全配置、用户输入或服务端代理管理，不进入 Git 历史。
- EPUB 内容视为不可信输入，限制脚本、外链和本地文件访问。
- 对词典包、模型包和更新包进行格式、大小和完整性校验。
- 日志不得记录书籍全文、完整原句、API 密钥或私人文件路径。
- 提供本地数据导出和彻底删除能力。

## 16. 跨平台要求

### 16.1 共享能力

以下逻辑应尽可能在三个平台共享：

- 数据模型；
- EPUB/PDF 阅读核心；
- 焦点加粗规则；
- 词典和翻译路由；
- 词卡；
- 设置；
- 国际化；
- 同步接口。

### 16.2 平台适配

桌面壳只负责平台相关能力：

- 文件系统访问；
- 原生窗口和全屏；
- 安全存储；
- 系统浏览器；
- 自动更新；
- 安装包与签名。

Web 端针对浏览器限制提供降级：

- 文件权限无法永久保持时要求重新授权；
- 全屏 API 不可用时进入页面内沉浸模式；
- 本地模型受内存或计算能力限制时降级到词典或在线翻译；
- 明确提示浏览器数据清理风险。

## 17. 性能与可靠性

### 17.1 目标

以近五年普通消费级电脑和现代 Chromium/Safari/Firefox 浏览器为基准：

- 应用冷启动目标小于 3 秒；
- Home 和书库本地数据首屏目标小于 1 秒；
- 已安装本地词典的单词查询 P95 小于 300ms；
- 焦点加粗开关后可见区域更新目标小于 150ms；
- 阅读进度保存不得阻塞翻页或滚动；
- 约 500 本书、10 万张词卡时，搜索仍可交互；
- 单个大文件处理必须显示进度并可取消。

### 17.2 稳定性

- 异常退出后恢复最近一次已保存位置。
- 写入进度和词卡使用原子事务。
- 数据迁移失败时保留可恢复备份。
- 不因单个损坏书籍或词典导致整个应用无法启动。

## 18. 知识产权与 GitHub 发布策略

### 18.1 Bionic Reading® 风险处理

官方资料表明 “Bionic Reading®” 方法涉及商标、版权和专利权，并提供 IP/API 商业授权。公开仓库不等于自动获得这些权利。

LexiAnchor 的默认路线：

1. 使用独立品牌 LexiAnchor。
2. 使用中性功能名 “Focus Highlighting / 焦点加粗阅读”。
3. 从公开的通用排版和可访问性原理独立设计实现。
4. 不复制官方算法参数、代码、字体、界面或品牌资产。
5. 不在 README 或应用商店文案中声明 “Bionic Reading compatible”。
6. 在面向法国等仍可能存在有效专利的市场分发前，进行专业专利权利要求比对。
7. 若希望直接使用该名称、字体、API 或官方方法，先取得书面授权。

### 18.2 GitHub 可见性

建议分阶段：

- **研发阶段**：私有仓库，完成技术验证和依赖审计。
- **公开预览阶段**：只公开独立实现，使用中性描述和免责声明。
- **正式发布阶段**：完成商标、专利、字体、词典、模型和第三方依赖审查。

仓库建议包含：

- `README.md`
- `LICENSE`
- `THIRD_PARTY_NOTICES.md`
- `CONTRIBUTING.md`
- `SECURITY.md`
- 字典和模型来源/许可证清单
- “Not affiliated with Bionic Reading AG” 等不构成兼容声明的关系说明（最终措辞需复核）

### 18.3 开源许可证

是否放在 GitHub 与是否开源是两件事。公开代码如果没有 LICENSE，通常仍保留全部版权。

候选策略：

- 研发期先保持私有，不急于选许可证；
- 确认公开后，可评估 Apache-2.0 或 MIT；
- Apache-2.0 含贡献者专利授权条款，但不能消除第三方专利风险；
- 字典、模型、字体和书籍样例必须分别遵守各自许可证，不能仅依赖项目主许可证。

最终许可证选择是公开发布门槛，不影响 MVP 本地开发。

## 19. 版本范围

### 19.1 P0：MVP

- Home、书库、词卡、设置；
- EPUB 2/3 阅读；
- 文本型 PDF 阅读；
- 阅读进度与继续阅读；
- 浅色、深色、护眼主题；
- 字体、字号、行距、词间距、字间距、正文宽度；
- 焦点加粗开关和三级强度；
- 内置英英、英法、英汉离线词典；
- 至少一种用户离线词典导入格式；
- 本地优先单词/短语/句子翻译框架；
- 在线翻译入口；
- Search on Web；
- 词卡创建、搜索、编辑、删除；
- 窗口和全屏模式；
- 中、英、法 UI；
- Windows、macOS、Web/PWA；
- 本地数据导出基础能力；
- 同步接口预留。

### 19.2 P1

- MDX/MDD 或更多用户词典格式；
- OCR 扫描 PDF；
- 更完善的本地双向翻译语言包；
- 全文搜索、书签、批注和高亮；
- 词卡标签、发音和导出；
- 自定义主题和排版预设；
- 自动备份；
- 正式跨设备同步；
- Linux 桌面端可行性评估。

### 19.3 P2

- 间隔重复复习；
- Anki 集成；
- 移动原生应用；
- 多用户和家庭共享；
- 可选云端书库；
- 插件或第三方提供者生态。

## 20. MVP 验收标准

### AC-01 导入与书库

- 用户可以在 Windows、macOS 和 Web 导入无 DRM EPUB/PDF。
- 导入成功后书籍出现在书库和 Home。
- 不支持或加密文件给出明确错误。

### AC-02 继续阅读

- 用户关闭并重新打开书籍后恢复到最近位置。
- Home 卡片显示与阅读器一致的百分比进度。
- 改变窗口大小或字号后仍回到相同语义位置附近。

### AC-03 焦点加粗

- 用户能即时开关焦点加粗并选择弱、中、强。
- 复制、搜索和屏幕阅读器使用原始文本。
- 开关操作不改变阅读进度或选择位置。

### AC-04 显示设置

- 三种主题均可使用。
- EPUB 的字号、行距、词间距和正文宽度实时生效并持久化。
- 全局默认和当前书籍覆盖逻辑清晰。

### AC-05 查词

- 离线状态下可查询英英、英法和英汉词典。
- 词典按用户设定顺序显示。
- 用户可以启停和重新排序词典。

### AC-06 翻译与搜索

- 用户选择单词、短语或句子后可打开翻译面板。
- 有本地能力时优先显示本地结果。
- 始终提供在线翻译和 Search on Web。
- 未经用户操作，不向在线服务发送文字。

### AC-07 词卡

- 用户可从查词结果创建词卡。
- 词卡保存词语、词性、英语释义、词根/词源、来源书籍、原句和创建时间。
- 用户可以搜索、编辑和删除词卡。

### AC-08 阅读模式

- 用户可在窗口和全屏模式之间切换。
- 切换后阅读位置保持不变。
- `Esc` 行为符合平台习惯。

### AC-09 国际化

- 系统语言为中文、英文或法文时首次启动使用相应 UI。
- 其他系统语言回退到英文。
- 用户可手动切换语言且设置被持久化。

### AC-10 本地优先

- 断网时仍可打开已导入书籍、保存进度、查本地词典和管理词卡。
- 未配置账号或云服务时不产生隐式网络同步。

### AC-11 无障碍

- 仅使用键盘可以完成导入后的核心阅读、查词、收藏和设置流程。
- 支持减少动态效果。
- 所有核心控件具有可访问名称和焦点状态。

### AC-12 数据恢复

- 正常退出和异常退出后均能恢复最近保存状态。
- 单个损坏文件不会阻止应用启动或访问其他书籍。

## 21. 风险与缓解

| 风险 | 影响 | 缓解措施 |
| --- | --- | --- |
| Bionic Reading® 商标/专利 | 公开分发或宣传可能产生侵权风险 | 中性命名、独立实现、发布前权利要求比对或取得授权 |
| 离线词典许可证 | 无法合法内置或分发 | 逐一记录来源和许可证，优先按需下载，提供用户自装 |
| 本地翻译模型体积和速度 | Web 或低配设备体验差 | 模型按需下载、量化、能力检测、在线翻译回退 |
| PDF 文本层质量差 | 选词和加粗错位 | 能力检测、逐页降级、明确提示、P1 OCR |
| Web 存储不稳定 | 浏览器清理导致数据丢失 | 导出/备份提示、持久化请求、未来同步 |
| 多平台差异 | 文件、全屏和字体行为不一致 | 共享核心 + 平台适配层 + 平台验收矩阵 |
| 词根数据缺失或错误 | 词卡质量下降 | 显示来源、允许为空和手动编辑、禁止猜测 |
| 阅读辅助效果因人而异 | 用户可能不适应默认规则 | 可关闭、三级强度、排版预设、避免医疗承诺 |

## 22. 已确认技术路线与保留调研

技术路线已记录于 [TECHNICAL_ARCHITECTURE.md](./TECHNICAL_ARCHITECTURE.md)，开发执行方式记录于 [DEVELOPMENT_WORKFLOW.md](./DEVELOPMENT_WORKFLOW.md)。

### 22.1 已确认

1. React + TypeScript + Vite 作为共享 UI 和业务技术栈。
2. Electron 作为 Windows/macOS 桌面壳。
3. Web 交付为 PWA，重型能力允许基于浏览器能力渐进增强。
4. PDF 使用 PDF.js。
5. Web 与桌面使用相同 SQLite schema、migration 和 Repository。
6. SQLite WASM 在 Worker 中运行并使用 OPFS 持久化。
7. 大型文件通过平台 ContentStore 保存，用户可见行为和导出格式一致。
8. v0.1 本地翻译使用 Mozilla Bergamot WASM 路线，EN→FR、EN→ZH 模型按需下载；
   ONNX Runtime 保留为未来可替换 Provider 候选。
9. 离线资源优先级为英英、英法、英汉。
10. 研发期使用私有 GitHub 仓库。
11. MVP 桌面包不签名，以便携 ZIP 交付，并接受系统安全警告限制。
12. `v0.1.0` 前直接在 `main` 开发，使用小提交、ADR 和自动质量门禁；第一版完成后重新评估短分支与 Pull Request。

### 22.2 必须完成的 Spike

1. Readium Web 与 EPUB.js 的兼容性、位置稳定性和样式注入对比。
2. PDF.js 文本层覆盖和焦点加粗可行性。
3. SQLite WASM + OPFS 在 Web/Electron 中的同 schema 验证。
4. WordNet、FreeDict 和英汉候选词典的数据质量及许可证审计。
5. StarDict 导入质量和索引性能。
6. EN→FR、EN→ZH 本地模型的许可证、体积、质量、速度和 Web 兼容性。
7. Windows/macOS 无签名包在干净设备上的实际安装摩擦。

### 22.3 公开发布门槛

1. 法国有效专利及目标发行国家的独立法律审查。
2. GitHub 主许可证、第三方 Notices 和 SBOM。
3. 词典、模型、字体和测试书籍的来源及许可证清单。
4. 无签名分发限制的准确说明；面向普通公众时重新评估签名。

## 23. 产品成功标准

由于 MVP 面向个人使用且默认不收集遥测，优先通过本地测试和用户自评衡量：

- 用户能在两分钟内完成首次导入并开始阅读；
- 查词到看到首个本地结果不打断阅读位置；
- 阅读过程中无需离开应用即可完成查词、翻译和词卡收藏；
- 在无网络环境中仍能完成主要阅读学习流程；
- 用户能找到舒适的主题、字号、行距和焦点强度组合；
- 连续使用一周后，阅读进度和词卡数据无丢失；
- 同一本书在窗口/全屏和不同窗口大小之间切换时位置稳定。

若未来引入遥测，必须默认关闭或明确征得同意，并优先采用本地统计。

## 24. 最终需求结论

LexiAnchor 的 MVP 是一款本地优先、无账号也可完整使用的跨平台英语学习阅读器。它以阅读连续性为核心，将 EPUB/PDF 阅读、焦点加粗、本地词典、本地优先翻译、网页搜索和词卡沉淀整合到同一个低干扰工作流中。

同步、OCR、复习系统和更多词典格式将在核心闭环稳定后逐步扩展。公开发布前必须完成第三方数据许可证审查，以及 Bionic Reading® 相关商标和专利风险评估。

## 25. 参考资料

- [Bionic Reading® 官方网站及知识产权声明](https://bionic-reading.com/)
- [Bionic Reading® IP/API 与商业授权说明](https://bionic-reading.com/br-pricing/)
- [Bionic Reading® FAQ](https://bionic-reading.com/faq/)
- [美国专利申请 US20170358238A1](https://patents.google.com/patent/US20170358238A1/en)
- [法国专利 FR3052587B1](https://patents.google.com/patent/FR3052587B1/en)
- [Emil Kowalski — Skills for Design Engineers](https://github.com/emilkowalski/skills)

专利数据库中的状态属于公开信息检索结果，不构成法律结论。正式跨地区公开分发前，应由具备相应司法辖区经验的专业人士复核。

## 26. 技术文档索引

- [技术架构](./TECHNICAL_ARCHITECTURE.md)
- [开发流程](./DEVELOPMENT_WORKFLOW.md)
- [ADR-0001：Electron、React 与 TypeScript](./docs/adr/0001-electron-react-typescript.md)
- [ADR-0002：本地优先存储](./docs/adr/0002-local-first-storage.md)
- [ADR-0003：阅读内核](./docs/adr/0003-reader-engines.md)
- [ADR-0004：词典与翻译](./docs/adr/0004-dictionaries-and-translation.md)
- [ADR-0005：无签名分发](./docs/adr/0005-unsigned-distribution.md)
